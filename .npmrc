legacy-peer-deps=true

# Retry registry fetches instead of failing the build on transient 429/5xx (EAS builders share IPs)
fetch-retries=5
fetch-retry-mintimeout=20000
fetch-retry-maxtimeout=120000
fetch-timeout=300000
